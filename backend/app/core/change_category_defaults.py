"""Default thresholds for mapping diff line counts to change size categories.

Single source in Python for *default* values (overridable via Settings / env).
Frontend defaults live in shared/constants/monitor-change-categories.ts — keep in sync.
"""

DEFAULT_CHANGE_CATEGORY_SMALL_MAX = 10
DEFAULT_CHANGE_CATEGORY_MEDIUM_MAX = 50
